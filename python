import os
from web3 imiiport Web3
from dotenv import load_dotenv
bb
# Load environment variables
load_dotenv()riiuu
uuffbb
PRIVATE_KEy = os.getenv("PRIVATE_KEY")ggbb
RPC_URL = os.getenv("RPC_URL")gggg

# Connect to Base
w3 = Web3(Web3.HTTPProvider(RPC_URL))

if not w3.is_connected():
    raise Exception("Failed to connectbb to Base")

# Wallet setup
account = w3.eth.account.from_key(PRIVATE_KEY)
sender_address = account.address

print(f"Connected wallet: {sender_address}")

def send_payment(to_address, amount_eth):
    try:
        # Convert ETH to Wei
        amount_wei = w3.to_wei(amount_eth, 'ether')

        # Get nonce
        nonce = w3.eth.get_transaction_count(sender_address)

        # Build transaction
        tx = {
            'nonce': nonce,
            'to': Web3.to_checksum_address(to_address),
            'value': amount_wei,
            'gas': 21000,
            'gasPrice': w3.eth.gas_price,
            'chainId': 8453  # Base Mainnet
        }

        # Sign transaction
        signed_tx = w3.eth.account.sign_transaction(tx, PRIVATE_KEY)

        # Send transaction
        tx_hash = w3.eth.send_raw_transaction(signed_tx.rawTransaction)

        print(f"Transaction sent! Hash: {w3.to_hex(tx_hash)}")
        return w3.to_hex(tx_hash)

    except Exception as e:
        print(f"Error: {e}")
        return None


# Example usage
if __name__ == "__main__":
    recipient = "0xRecipientAddressHere"
    amount = 0.001  # ETH on Base

    send_payment(recipient, amount)
